import 'package:flutter/material.dart';

class MyPageFollowPage extends StatelessWidget {
  const MyPageFollowPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
